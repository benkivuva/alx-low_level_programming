#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints a specific message using the function puts
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* Prints the specified message */
	puts("Programming is like building a multilingual puzzle");

	return (0);
}
