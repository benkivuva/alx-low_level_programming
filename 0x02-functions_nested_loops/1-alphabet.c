#include "main.h"

/**
 * print_alphabet - prints alphabet in lowercase
 *
 * Return: Always 0
 */
void print_alphabet(void)
{
	char lower;

	for (lower = 'a'; lower = < 'z'; a++)
	_putchar(lower);
	_putchar("\n");
}
